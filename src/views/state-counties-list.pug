extends layout

block append header
  - var flowStep = "requirements" // location · requirements · reminder · request
  include components/flow-progress.pug

block content
  ul
    each county in counties
      li
        a(href='/'+county.url)= county.countyName
